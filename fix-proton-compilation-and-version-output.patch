diff -ruN wine-proton/configure.ac wine-proton-new/configure.ac
--- wine-proton/configure.ac	2026-05-29 07:54:13.774833837 +0000
+++ wine-proton-new/configure.ac	2026-05-29 07:54:41.116484035 +0000
@@ -2513,6 +2513,8 @@
 
 wine_srcdir=
 test "$srcdir" = . || wine_srcdir="$srcdir/"
+GIT_DIR=${wine_srcdir}.git git rev-parse --short HEAD 2>/dev/null && wine_git_revision=-g"$(GIT_DIR=${wine_srcdir}.git git rev-parse --short HEAD)"
+wine_version_suffix=" (Proton)"
 
 WINE_CONFIG_MAKEFILE(dlls/acledit)
 WINE_CONFIG_MAKEFILE(dlls/aclui)
@@ -3847,7 +3849,7 @@
 
 WINE_APPEND_RULE(
 [dlls/ntdll/unix/version.c: dummy
-	@version=\`(GIT_DIR=${wine_srcdir}.git git describe HEAD 2>/dev/null || echo \"wine-\$(PACKAGE_VERSION)\") | sed -n -e '\$\$s/\(.*\)/const char wine_build[[]] = \"\\1\";/p'\` && (echo \$\$version | cmp -s - \$[@]) || echo \$\$version >\$[@] || (rm -f \$[@] && exit 1)
+	@version=\`(echo \"wine-\$(PACKAGE_VERSION)$wine_git_revision$wine_version_suffix\") | sed -n -e '\$\$s/\(.*\)/const char wine_build[[]] = \"\\1\";/p'\` && (echo \$\$version | cmp -s - \$[@]) || echo \$\$version >\$[@] || (rm -f \$[@] && exit 1)
 programs/winetest/build.rc: dummy
 	@build=\"STRINGTABLE { 1 \\\"\`GIT_DIR=${wine_srcdir}.git git rev-parse HEAD 2>/dev/null\`\\\" }\" && (echo \$\$build | cmp -s - \$[@]) || echo \$\$build >\$[@] || (rm -f \$[@] && exit 1)
 dlls/wineandroid.drv/wine-debug.apk: dlls/wineandroid.drv/build.gradle ${wine_srcdir}dlls/wineandroid.drv/AndroidManifest.xml ${wine_srcdir}dlls/wineandroid.drv/WineActivity.java ${wine_srcdir}dlls/wineandroid.drv/wine.svg
diff -ruN wine-proton/dlls/nsiproxy.sys/Makefile.in wine-proton-new/dlls/nsiproxy.sys/Makefile.in
--- wine-proton/dlls/nsiproxy.sys/Makefile.in	2026-05-29 07:54:13.229171929 +0000
+++ wine-proton-new/dlls/nsiproxy.sys/Makefile.in	2026-05-29 07:55:36.941890199 +0000
@@ -1,7 +1,7 @@
 MODULE    = nsiproxy.sys
 UNIXLIB   = nsiproxy.so
 IMPORTS   = ntoskrnl
-UNIX_LIBS = $(PROCSTAT_LIBS)
+UNIX_LIBS = $(PROCSTAT_LIBS) -lpthread
 
 EXTRADLLFLAGS = -Wl,--subsystem,native
 
diff -ruN wine-proton/dlls/ntdll/unix/signal_x86_64.c wine-proton-new/dlls/ntdll/unix/signal_x86_64.c
--- wine-proton/dlls/ntdll/unix/signal_x86_64.c	2026-05-29 07:54:13.235781570 +0000
+++ wine-proton-new/dlls/ntdll/unix/signal_x86_64.c	2026-05-29 07:54:41.118484229 +0000
@@ -109,6 +109,8 @@
  */
 #ifdef linux
 
+#define MAP_FIXED_NOREPLACE 0x100000
+
 #include <asm/prctl.h>
 static inline int arch_prctl( int func, void *ptr ) { return syscall( __NR_arch_prctl, func, ptr ); }
 
diff -ruN wine-proton/dlls/opencl/Makefile.in wine-proton-new/dlls/opencl/Makefile.in
--- wine-proton/dlls/opencl/Makefile.in	2026-05-29 07:54:13.261701948 +0000
+++ wine-proton-new/dlls/opencl/Makefile.in	2026-05-29 07:54:41.122274765 +0000
@@ -1,6 +1,6 @@
 MODULE    = opencl.dll
 UNIXLIB   = opencl.so
-UNIX_LIBS = $(OPENCL_LIBS)
+UNIX_LIBS = -lOpenCL
 
 SOURCES = \
 	pe_thunks.c \
